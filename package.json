{
  "name": "letsencrypt-route53-cli",
  "version": "0.0.1",
  "description": "Command-line interface to encrypt message using github users public ssh keys",
  "main": "lib/letsencrypt-route53.js",
  "bin": {
    "letsencrypt-route53": "lib/letsencrypt-route53.js"
  },
  "license": "MIT",
  "scripts": {
    "build": "babel --presets es2015,stage-0 src --out-dir lib",
    "prepublish": "babel --presets es2015,stage-0 src --out-dir lib",
    "watch": "babel --watch src --out-dir lib"
  },
  "dependencies": {
    "aws-sdk": "^2.3.5",
    "commander": "^2.9.0",
    "babel-polyfill": "^6.8.0"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-core": "^6.8.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-preset-stage-3": "^6.5.0"
  }
}
